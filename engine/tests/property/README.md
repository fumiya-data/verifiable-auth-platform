# Property Tests

This directory is reserved for future property-based tests that exercise the
engine against higher-volume randomized state transitions.

Until executable property tests are added, the canonical semantic coverage
remains:

- Lean state-machine proofs in `spec/`
- C unit and integration tests in `engine/tests`
- Contract notes and golden outputs in `tests/`
